"""JWT issuance (local/dev mode) and validation (local or real OIDC),
shared by the FastAPI service, the .NET gateway, and the MCP server's write
tools. See jwt_service.py for the full design note.
"""
